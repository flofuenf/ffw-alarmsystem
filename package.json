{
  "name": "ffw-alarmsystem",
  "version": "1.0.0",
  "private": true,
  "description": "Alarmsystem fuer die freiwillige Feuerwehr (Planung, Alarmierung, Alarmmonitor)",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "postinstall": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n SERVER,CLIENT -c green,cyan \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server",
    "setup:piper": "node scripts/setup-piper.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
